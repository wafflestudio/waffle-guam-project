  apiVersion: v1

  kind: Service

  metadata:

    name: guam-svc

  spec:

    selector:

      app: guam

    ports:

      - port: 80

        protocol: TCP

        targetPort: 8080

    type: LoadBalancer
