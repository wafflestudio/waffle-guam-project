package waffle.guam

import org.gradle.api.Project
import org.gradle.testfixtures.ProjectBuilder
import org.junit.Test
import waffle.guam.docker.DockerBuildTask

class WafflePluginTest {
//    @Test
//    fun test() {
//        val pluginProject: Project = ProjectBuilder.builder().build()
//        pluginProject.pluginManager.apply("waffle.gradle.waffle-plugin")
//        val task = pluginProject.tasks.findByName("dockerBuild")
//        assert(task != null)
//        assert(task is DockerBuildTask)
//    }
}